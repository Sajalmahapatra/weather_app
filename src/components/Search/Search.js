import React from 'react'
import { SearchElement, SearchIcon, SearchInput } from './styled'

const Search = () => {
  return (
    <SearchElement>
        <SearchIcon/>
        <SearchInput/>
    </SearchElement>
  )
}

export default Search